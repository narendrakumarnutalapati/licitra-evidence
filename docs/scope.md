## Scope (6-month deliverables)

- FastAPI + Postgres ledger (core repo)
- Per-organization hash chain logging (SHA-256)
- Deterministic canonical JSON hashing + test vectors
- Verification endpoint + tamper/replay experiments
- Rollback v0 circuit breaker decision record
- Incident evidence export formats (JSON/PDF)

This repo is the reproducible reference slice used for grant + evaluation.
